AUTH_SECRET=          # same value as api/.env and the app's setup screen
USER_ID=me
MONGO_URI=            # existing Atlas connection string — the same database local dev uses
MONGO_DB=             # existing database name
OPENROUTER_API_KEY=   # ticket 10
REPLY_MODEL=
ASSESSMENT_MODEL=
NARRATIVE_MODEL=
DRAFT_MODEL=
DRILL_GEN_MODEL=
DRILL_JUDGE_MODEL=
LANGSMITH_TRACING=false
LANGSMITH_API_KEY=
LANGSMITH_PROJECT=language-learning
