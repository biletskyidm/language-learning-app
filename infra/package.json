{
  "name": "infra",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "synth": "cdk synth",
    "deploy": "cdk deploy",
    "test": "vitest run",
    "lint": "eslint bin lib test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.250.0",
    "constructs": "^10.4.2"
  },
  "devDependencies": {
    "@types/node": "^24.5.0",
    "aws-cdk": "^2.1118.2",
    "esbuild": "^0.25.10",
    "tsx": "^4.20.5",
    "vitest": "^3.2.4"
  }
}
